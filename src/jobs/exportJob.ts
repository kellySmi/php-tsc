

export default function exportJob(data: any[]){ //id: string) {
     //const rtnAr  = [`The Export job says the id is ${id}`, `The Import job says the id is ${id}`];

    return console.log(JSON.stringify(data));
}
// function doSomeStuff(id: string){
//     return `The Export job says the id is ${id}`;
// }
